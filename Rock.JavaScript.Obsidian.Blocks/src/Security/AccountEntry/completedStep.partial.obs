<!-- Copyright by the Spark Development Network; Licensed under the Rock Community License -->
<template>
    <div>
        <div v-if="options.isPlainCaption && options.caption" class="aeCompletedBox">
            <p>{{ options.caption }}</p>
        </div>
        <div v-else-if="options.caption" class="aeCompletedBox">
            <p>{{ options.caption }}</p>
        </div>

        <div v-if="options.redirectUrl" class="aeActions">
            <button class="aeSubmitBtn" :disabled="disabled" type="button" @click="onContinueClicked">
                Continuar
            </button>
        </div>
    </div>
</template>

<script setup lang="ts">
    import { onMounted, PropType } from "vue";
    import { AccountEntryCompletedStepBag } from "@Obsidian/ViewModels/Blocks/Security/AccountEntry/accountEntryCompletedStepBag";

    const props = defineProps({
        disabled: {
            type: Boolean as PropType<boolean>,
            required: false,
            default: false
        },
        options: {
            type: Object as PropType<AccountEntryCompletedStepBag>,
            required: true
        }
    });

    const emit = defineEmits<{
        (e: "navigate", value: string): void
    }>();

    //#region Event Handlers

    function onContinueClicked(): void {
        tryNavigate(props.options.redirectUrl);
    }

    //#endregion

    //#region Functions

    function tryNavigate(url: string | null | undefined): void {
        if (url) {
            emit("navigate", url);
        }
    }

    //#endregion

    onMounted(() => {
        if (props.options.isRedirectAutomatic) {
            tryNavigate(props.options.redirectUrl);
        }
    });
</script>
